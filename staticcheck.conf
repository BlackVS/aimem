# ST1005 (error strings: no capitals/punctuation/newlines) is excluded:
# this CLI deliberately returns multi-line usage texts as errors
# (cmd/aimem/docs.go, col.go) — that convention stays.
checks = ["inherit", "-ST1005"]
